import type{MetadataRoute}from"next";import{prisma}from"@/app/lib/prisma";import{publicProductPolicy}from"@/app/lib/product-visibility";export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.NEXT_PUBLIC_SITE_URL??"http://localhost:3000";const[products,categories,brands,stores]=await Promise.all([prisma.product.findMany({where:publicProductPolicy,select:{slug:true,updatedAt:true}}),prisma.category.findMany({where:{active:true},select:{slug:true,updatedAt:true}}),prisma.brand.findMany({where:{active:true},select:{slug:true,updatedAt:true}}),prisma.sellerProfile.findMany({where:{status:"APPROVED"},select:{storeSlug:true,updatedAt:true}})]);return[{url:base,lastModified:new Date()},{url:`${base}/markalar`,lastModified:new Date()},...products.map((x)=>({url:`${base}/urun/${x.slug}`,lastModified:x.updatedAt})),...categories.map((x)=>({url:`${base}/kategori/${x.slug}`,lastModified:x.updatedAt})),...brands.map((x)=>({url:`${base}/marka/${x.slug}`,lastModified:x.updatedAt})),...stores.map((x)=>({url:`${base}/magaza/${x.storeSlug}`,lastModified:x.updatedAt}))]}
