import { auth } from "@/auth";
import { CampaignParticipationError, revokeCampaignParticipation } from "@/app/lib/campaign-participation";
export async function POST(request: Request, { params }: { params: Promise<{ participationId: string }> }) { const session = await auth(); if (!session?.user?.id) return Response.json({ error: "UNAUTHENTICATED" }, { status: 401 }); if (session.user.role !== "ADMIN") return Response.json({ error: "FORBIDDEN" }, { status: 403 }); try { return Response.json(await revokeCampaignParticipation(session.user.id, (await params).participationId, await request.json().catch(() => null))); } catch (error) { if (error instanceof CampaignParticipationError) return Response.json({ error: error.code }, { status: error.code === "FORBIDDEN" ? 403 : error.code === "PARTICIPATION_NOT_FOUND" ? 404 : ["STALE_VERSION", "TERMINAL_PARTICIPATION", "TARGET_MISMATCH", "CONCURRENT_CHANGE"].includes(error.code) ? 409 : 400 }); throw error; } }
