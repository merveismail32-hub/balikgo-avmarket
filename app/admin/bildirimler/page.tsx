import{AdminShell}from"@/app/components/admin-shell";import{requireAdmin}from"@/app/lib/admin-auth";import{prisma}from"@/app/lib/prisma";export default async function Page(){await requireAdmin();const rows=await prisma.notification.findMany({select:{id:true,type:true,channel:true,status:true,title:true,createdAt:true,userId:true,sellerId:true,order:{select:{orderNumber:true}}},orderBy:{createdAt:"desc"},take:20});return <AdminShell title="Bildirim Outbox"><div className="space-y-2">{rows.map(x=><div key={x.id} className="rounded-xl border bg-white p-4"><b>{x.title}</b><p className="text-sm text-slate-500">{x.type} · {x.channel} · {x.status} · {x.order?.orderNumber??"—"}</p></div>)}</div></AdminShell>}
