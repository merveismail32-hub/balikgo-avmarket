import{AdminShell}from"@/app/components/admin-shell";import{AdminSimpleCreate}from"@/app/components/admin-simple-create";import{requireAdmin}from"@/app/lib/admin-auth";import{prisma}from"@/app/lib/prisma";export default async function Page(){await requireAdmin();const rows=await prisma.category.findMany({select:{id:true,name:true,slug:true,active:true,sortOrder:true,parent:{select:{name:true}},_count:{select:{products:true}}},orderBy:[{sortOrder:"asc"},{name:"asc"}],take:50});return <AdminShell title="Kategoriler"><AdminSimpleCreate kind="categories"/><div className="mt-5 divide-y rounded-2xl border bg-white">{rows.map(x=><div key={x.id} className="flex justify-between p-4"><span><b>{x.name}</b><small className="block text-slate-500">/{x.slug} · Üst: {x.parent?.name??"—"}</small></span><span>{x.active?"Aktif":"Pasif"} · {x._count.products} ürün</span></div>)}</div><p className="mt-4 text-sm text-slate-500">Hard delete sunulmaz; bağlı ürün geçmişi korunur.</p></AdminShell>}
