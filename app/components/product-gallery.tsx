"use client";
import Image from "next/image";
import { useState } from "react";
export function ProductGallery({ name, image, images }: { name: string; image: string; images: string[] }) { const gallery = Array.from(new Set([image, ...images].filter(Boolean))); const [selected, setSelected] = useState(0); const active = gallery[selected] ?? image; return <div><div className="relative aspect-square overflow-hidden rounded-3xl border bg-white"><Image src={active} alt={`${name} ürün görseli`} fill priority sizes="(max-width: 1023px) calc(100vw - 2.5rem), 50vw" className="object-contain p-6" /></div>{gallery.length > 1 && <div className="mt-4 grid grid-cols-4 gap-3">{gallery.map((src, index) => <button key={`${src}-${index}`} type="button" aria-label={`${name} ${index + 1}. görselini göster`} aria-pressed={selected === index} onClick={() => setSelected(index)} className={`relative aspect-square overflow-hidden rounded-xl border-2 bg-white focus:outline-none focus:ring-2 focus:ring-sky-500 ${selected === index ? "border-sky-500" : "border-transparent hover:border-sky-300"}`}><Image src={src} alt="" fill sizes="120px" className="object-contain p-2" /></button>)}</div>}</div>; }
