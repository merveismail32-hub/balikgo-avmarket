"use client";import Image from"next/image";import Link from"next/link";import{FavoriteButton}from"./favorite-button";import type{Product}from"@/app/lib/products";
export function ProductCard({product}:{product:Product}){return <article className="group relative overflow-hidden rounded-2xl border bg-white shadow-sm transition hover:-translate-y-1 hover:border-sky-300 hover:shadow-lg"><FavoriteButton product={product} className="absolute right-3 top-3 z-10 flex h-10 w-10 items-center justify-center rounded-full bg-white shadow"/><Link href={`/urun/${product.slug??product.id}`}><div className="relative aspect-square bg-slate-50"><Image src={product.image} alt={product.name} fill sizes="(max-width:640px) 50vw,(max-width:1024px) 33vw,25vw" className="object-contain p-5 transition group-hover:scale-105"/></div><div className="p-4"><p className="text-xs font-semibold text-slate-500">{product.brand}{product.sellerName?` · ${product.sellerName}`:""}</p><h2 className="mt-1 line-clamp-2 min-h-12 font-black group-hover:text-sky-700">{product.name}</h2>{product.reviewCount>0?<p className="mt-2 text-sm text-amber-600">★ {product.rating.toFixed(1)} <span className="text-slate-400">({product.reviewCount})</span></p>:<p className="mt-2 text-sm text-slate-400">Yeni ürün</p>}<p className="mt-3 text-xl font-black">{product.price}</p>{(product.stock??0)>0?<p className="mt-2 text-xs font-bold text-emerald-700">Stokta</p>:<p className="mt-2 text-xs font-bold text-red-700">Stokta yok</p>}</div></Link></article>}
