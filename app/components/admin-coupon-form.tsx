"use client";import{FormEvent,useState}from"react";export function AdminCouponForm(){const[msg,setMsg]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const body={code:f.get("code"),name:f.get("name"),scope:"PLATFORM",discountType:f.get("type"),discountValue:Number(f.get("value")),active:true};const r=await fetch("/api/admin/coupons",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const b=await r.json().catch(()=>({}))as{error?:string};setMsg(r.ok?"Kupon oluşturuldu; sayfayı yenileyebilirsiniz.":b.error??"Hata")}return <form onSubmit={submit} className="grid gap-2 rounded-2xl border bg-white p-4 sm:grid-cols-4"><input name="code" required maxLength={50} placeholder="KOD" className="rounded-xl border p-3"/><input name="name" required placeholder="Kupon adı" className="rounded-xl border p-3"/><select name="type" className="rounded-xl border p-3"><option value="PERCENTAGE">Yüzde</option><option value="FIXED">Sabit TL</option></select><input name="value" required type="number" min="0.01" step="0.01" placeholder="Değer" className="rounded-xl border p-3"/><button className="rounded-xl bg-slate-950 p-3 font-bold text-white">Kupon oluştur</button>{msg&&<p role="status" className="sm:col-span-3">{msg}</p>}</form>}
