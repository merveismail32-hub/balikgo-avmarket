"use client";import{FormEvent,useState}from"react";export function AdminSimpleCreate({kind}:{kind:"categories"|"brands"}){const[name,setName]=useState("");const[msg,setMsg]=useState("");async function submit(e:FormEvent){e.preventDefault();const r=await fetch(`/api/admin/${kind}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name,...(kind==="categories"?{sortOrder:0}:{})})});const b=await r.json().catch(()=>({}))as{error?:string};setMsg(r.ok?"Kayıt oluşturuldu; sayfayı yenileyebilirsiniz.":b.error??"İşlem başarısız.");if(r.ok)setName("")}return <form onSubmit={submit} className="flex flex-wrap gap-2"><label className="sr-only" htmlFor={`${kind}-name`}>Ad</label><input id={`${kind}-name`} value={name} onChange={e=>setName(e.target.value)} required minLength={2} maxLength={100} placeholder={kind==="categories"?"Kategori adı":"Marka adı"} className="flex-1 rounded-xl border p-3"/><button className="rounded-xl bg-slate-950 px-5 font-bold text-white">Ekle</button>{msg&&<p role="status" className="w-full text-sm text-slate-500">{msg}</p>}</form>}
