"use client";
import Link from "next/link";
import { FormEvent, useState } from "react";

export default function SellerApplicationPage() {
  const [message, setMessage] = useState(""); const [error, setError] = useState(""); const [sending, setSending] = useState(false);
  async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setError(""); setMessage(""); setSending(true); const form = new FormData(event.currentTarget); const body = { ...Object.fromEntries(form), acceptedTerms: form.get("acceptedTerms") === "true" }; const response = await fetch("/api/seller-applications", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(body) }); const data = await response.json().catch(() => ({})) as { error?: string }; setSending(false); if (!response.ok) { setError(data.error ?? "Başvuru gönderilemedi."); return; } setMessage("Başvurunuz alındı. Onaylandığında satıcı panelinizi kullanabilirsiniz."); }
  const input = "mt-2 w-full rounded-xl border border-slate-200 bg-white px-4 py-3.5 text-sm outline-none focus:border-sky-500";
  return <main className="min-h-screen bg-slate-50 text-slate-900"><div className="bg-slate-950 px-5 py-2 text-center text-sm font-medium text-white">🎣 BalıkGo Satıcı Merkezi</div><header className="border-b bg-white"><div className="mx-auto flex max-w-5xl items-center justify-between gap-4 px-5 py-5"><Link href="/" className="min-w-fit"><div className="text-2xl font-black text-sky-600">BALIK<span className="text-slate-950">GO</span></div><div className="text-[10px] font-bold uppercase tracking-[0.25em] text-slate-500">Satıcı Merkezi</div></Link><Link href="/" className="rounded-xl px-4 py-3 text-sm font-bold transition hover:bg-slate-100">🏠 Ana Sayfa</Link></div></header><section className="mx-auto max-w-4xl px-5 py-12 sm:py-16"><div className="rounded-3xl border bg-white p-6 shadow-sm sm:p-10"><p className="font-bold text-sky-600">MAĞAZANI BÜYÜT</p><h1 className="mt-2 text-3xl font-black sm:text-4xl">Satıcı Başvurusu</h1><p className="mt-3 max-w-2xl leading-7 text-slate-500">Başvurunuz hesabınıza bağlanır; değerlendirme sonucu bu hesap için geçerli olur.</p><form onSubmit={submit} className="mt-8 grid gap-5 sm:grid-cols-2"><label className="block text-sm font-bold sm:col-span-2">Mağaza adı<input required name="storeName" className={input} /></label><label className="block text-sm font-bold">Yetkili adı<input required name="contactName" className={input} /></label><label className="block text-sm font-bold">Yetkili soyadı<input required name="contactSurname" className={input} /></label><label className="block text-sm font-bold">E-posta<input required name="contactEmail" type="email" className={input} /></label><label className="block text-sm font-bold">Telefon<input required name="phone" type="tel" minLength={10} className={input} /></label><label className="block text-sm font-bold">Şirket türü<select name="companyType" className={input}><option>Şahıs şirketi</option><option>Limited şirket</option><option>Anonim şirket</option></select></label><label className="block text-sm font-bold">Vergi numarası<input required name="taxNumber" className={input} /></label><label className="block text-sm font-bold">Vergi dairesi<input required name="taxOffice" className={input} /></label><label className="block text-sm font-bold">Şehir<input required name="city" className={input} /></label><label className="block text-sm font-bold sm:col-span-2">Açık adres<textarea required name="address" rows={3} className={input} /></label><label className="block text-sm font-bold sm:col-span-2">Satılan ürün kategorileri<input required name="categories" className={input} /></label><label className="block text-sm font-bold sm:col-span-2">Mağaza açıklaması<textarea required name="description" rows={4} className={input} /></label><label className="flex items-start gap-3 text-sm leading-6 text-slate-600 sm:col-span-2"><input required name="acceptedTerms" value="true" type="checkbox" className="mt-1 h-4 w-4" /><span>Satıcı sözleşmesini okudum ve onaylıyorum.</span></label>{error && <p className="rounded-xl bg-red-50 p-3 text-sm font-bold text-red-600 sm:col-span-2">{error}</p>}{message && <p className="rounded-xl bg-green-50 p-3 text-sm font-bold text-green-700 sm:col-span-2">{message}</p>}<button disabled={sending} className="rounded-xl bg-sky-500 py-4 font-bold text-white shadow-lg shadow-sky-500/20 transition hover:bg-sky-600 disabled:opacity-60 sm:col-span-2">{sending ? "Gönderiliyor..." : "Başvuruyu Gönder"}</button></form></div></section></main>;
}
