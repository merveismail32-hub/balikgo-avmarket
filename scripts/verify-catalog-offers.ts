// Backward-compatible command entrypoint. The shared harness enforces the TEST
// database identity, official CA verification and exact-ID-only cleanup.
import "./test-catalog-intelligence-db-e2e.ts";
